// Бизнес логика

class WeatherModel {
    constructor(baseUrl, apiKey){
        this.apiData = {
            url : baseUrl, 
            api: apiKey
        }
    }

    async fetchWeatherData(queryParams) {
        appid
    }
}